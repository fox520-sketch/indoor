# 室內導航工具箱（改良版）

這是可直接上傳 GitHub 的單檔前端版本。

## 本次已實作

- GPS 柔性位置校正
- 地圖縮放、拖曳、雙擊縮放
- 指北針與縮放比例顯示
- 在地圖上直接新增標定點
- 以 GPS 建立標定點
- 以標定點校正目前位置
- GPS + 步頻自動估算步長（平滑更新）

## 使用方式

直接開啟 `index.html` 即可。
建議使用手機瀏覽器測試感測器、方向與定位能力。

## 注意

- 目前 QR 產生仍使用外部服務產圖。
- GPS 在室內精度受環境影響很大，因此本版採用柔性融合，不做硬跳點。
- 這是前端原型，資料儲存在 localStorage。
\n- v6: 目前位置新增標定點改成以紫色外環固定顯示；停止背景自動 GPS 柔性校正，避免已建立的 GPS 固定標定點看起來隨後續 GPS 漂移。\n
- v10: 目前位置標定點不再與紅點重疊，改為在紅點旁以紫色浮動標記顯示，並用連線指出對應位置。

- v11: 移除「以目前位置新增標定點」與「以 GPS 新增標定點」按鈕，改為直接點導航地圖新增軌跡點；點既有軌跡點可查看座標與距起點軌跡距離。

- v12: 修正導航地圖新增軌跡點的點擊/觸控事件，避免手機上只有拖曳沒有建立點；清除舊版已移除功能的殘留事件。

- v13: 地圖縮放最小值從 20% 調整為 1%。

- v16: 實際修正 index.html 內嵌腳本與 app.js，同步把最小縮放值改為 1%。

- v17: 修正 index.html 內嵌腳本的 MIN_VIEW_SCALE，最小縮放改為 1%。

- v18: 在地圖編輯模式下同步顯示導航軌跡、起點與目前位置。

- v19: 同步修正 index.html 內嵌腳本與 app.js，讓地圖編輯模式顯示起點、目前位置與軌跡。

- v20: 導航地圖與地圖編輯模式新增尺規，並顯示起點到目前位置的距離。

- v21: 補上導航模式的尺規與起點到目前位置距離顯示。

- v22: 在導航地圖上方新增即時軌跡資訊面板，顯示移動距離、方向、速度、平均速度、距起點距離與目前位置座標。

- v23: 明確更新 app.js 與 index.html，並在導航資訊面板加入標題「軌跡資訊面板 v23」。

- v24: 導航模式 UI 優化，將中央資訊面板改成上方資訊條，座標改為右上角小膠囊顯示，避免遮住地圖中心。

- v25: 導航模式改成 HTML 資訊條，資訊不再畫在 canvas 上；座標改成右下角膠囊顯示。

- v25b: 修正 index.html 開頭標記位置錯誤，恢復頁面正常顯示。

- v26: 新增視覺軌跡平滑，保留原始軌跡資料不變，只在導航與地圖編輯顯示平滑後的路線。

- v27: 新增真正定位平滑，對目前位置與方向更新做低通濾波，讓紅點與資訊面板更穩定。

- v28: 新增可調平滑強度，提供靈敏、標準、穩定三段切換，並記住使用者選擇。

- v29: 將平滑強度三段切換改為滑桿，可在 0.05 到 0.50 間細調，並即時顯示當前強度。

- v30: 導航地圖的「立即回正」改為「目前位置」，按下後保持當前倍率不變，並把目前位置移到地圖中心。

- v31: 修正「目前位置」按鈕的置中公式，改用世界座標平移，讓目前位置正確回到地圖中心且倍率不變。

- v32: 補上目前位置置中的 helper 與事件處理，按下「目前位置」會在維持倍率下把目前位置移到導航地圖中心。

- v34 rebuild: 目前位置座標改為整數公尺顯示（四捨五入）。

- v35: 導航地圖上方的「目前座標」改為顯示整數公尺（四捨五入）。

- v36: 導航模式下，當「自動回正：關」時，位置更新會自動平移地圖，讓目前位置維持在中央，倍率不變。

- v37: 新增「跟隨目前位置：開/關」獨立開關，與「自動回正」分開控制。

- v38: 修正導航地圖底部按鈕在自動回正關閉時的排版擁擠問題，並避免目前位置與指南針過度重疊。

- v39: 自動回正時增加上下邊界留白，避免起點終點貼邊；底部按鈕改為短標籤並固定排在地圖下方。

- v40: 修正導航地圖底部工具列消失問題，按鈕固定顯示在地圖下方，保留短標籤。

- v41: 導航模式工具列改為固定顯示在地圖下方，不再疊在地圖內，解決按鈕消失與遮擋問題。

- v42: 修正地圖下方空白，地圖自動撐滿可用高度，工具列貼齊下方。

- v43: 增加自動回正時的上下留白，避免起點與目前位置貼近地圖上下邊界。

- v44: 重做自動回正的安全區算法，為上方、右上指南針區與下方保留內邊界，避免起點與目前位置貼邊或被遮擋。

- v46: QR 產生器會自動以導航地圖目前位置的 X、Y、方向預填 QR 校正點欄位，使用者仍可手動修改。

- v48: 修正 QR 產生器欄位 id，並在 QR 表單按鈕列新增「用目前位置」按鈕；切到 QR 頁也會自動帶入目前位置。

- v49: 導航模式新增「匯入 JSON」按鈕，可匯入軌跡並以不同顏色顯示，不影響目前導航軌跡。

- v50: 修正「匯入 JSON」按鈕無法開啟檔案選擇器的問題，補上 type=button 與更穩定的點擊處理。

- v51: 修正匯入 JSON 檔案選擇器，改用原生 label-for 方式開啟檔案選擇器，並補上 importJsonFile 的 change 事件。

- v52: 修正「匯入 JSON」解析，支援匯入本工具自己匯出的 JSON 格式，並顯示匯入的軌跡點數。

- v53: 修正匯入 JSON 流程，選檔後一定會在訊息列顯示結果，方便判斷是未讀到檔案還是格式不符。

- v54: 匯入 JSON 改成更穩定的原生 label + file input 流程，並同時監聽 change/input 事件，確保選檔後有回應。

- v55: 修正頁面頂部誤顯示的 \n 文字，並讓「匯入 JSON」按鈕外觀與其他按鈕一致；同時改用更穩定的 delegated 匯入事件。

- v56: 修正匯入 JSON 事件作用域錯誤，將選檔與匯入事件綁定移回主程式內，讓 setMessage 與 importNavJsonTracks 可正常執行。

- v57: 同步修正 app.js 與 index.html 內嵌主程式的匯入 JSON 邏輯，確保選檔、訊息列、軌跡解析與顯示都在實際執行的程式中生效。

- v58: 修正導航模式重複出現 2 個「匯入 JSON」按鈕的問題，保留單一可用按鈕。

- v59: 導航模式新增「修改方向」按鈕，可手動輸入手機行進方向（0~359）。
